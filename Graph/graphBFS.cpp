#include<bits/stdc++.h>
funt(){
    unordered_map<int, list<int>> adj

    for(all component of graph)
    if(not visited){
        bfs();
    }
}

bfs(){
    queue<int> q;
    q.push(node);
    visited[node] = 1;

    while(q not empty)
    {
        front = q.front
        q.pop
        ans.pushback(front);
        for(all neighbours of adj(front))
        if(not visited)
        {
            q.pus(i);
            vis[i] = 1
        }
    }
}

dfs(){

    component.push(node);
    visited[node] = 1;

    for(all neighbours){
        call dfs if not visited
    }
}

